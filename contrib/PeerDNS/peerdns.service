[Unit]
Description=PeerDNS daemon
Wants=network.target
After=network.target

[Service]
Type=simple
ExecStart=cd /home/__USER__/PeerDNS && mix run --no-halt
ExecStop=!!!TODO!!!

[Install]
WantedBy=multi-user.target