[Unit]
Description=Scuttlebot (SSB) daemon
Wants=network.target
After=network.target

[Service]
User=_USER_
Group=_USER_
Type=simple
ExecStart=/usr/bin/sbot server 
ExecStop=/usr/bin/pkill -f sbot
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
