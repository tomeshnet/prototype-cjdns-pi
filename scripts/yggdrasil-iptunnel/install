#!/usr/bin/env bash

set -e

BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

sudo cp "$BASE_DIR/yggdrasil-setup" "/usr/local/sbin/yggdrasil-setup"
sudo mkdir /etc/systemd/system/unit.d || true
sudo cp "$BASE_DIR/yggdrasil-iptunnel.service" /etc/systemd/system/unit.d/yggdrasil.service

sudo chmod a+x /usr/local/sbin/yggdrasil-setup

sudo systemctl daemon-reload